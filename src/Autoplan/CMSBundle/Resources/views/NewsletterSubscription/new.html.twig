{% extends 'AutoplanCMSBundle::layout.html.twig' %}

{% block content %}

    <ol class="breadcrumb bc-3">
        <li>
            <a href="{{ path('admin_dashboard') }}"><i class="entypo-home"></i>Home</a>
        </li>
        <li>
            <a href="{{ path('admin_newsletter_subscribers') }}">Newsletter Subscribers</a>
        </li>
        <li class="active">

            <strong>Add Subscriber</strong>
        </li>
    </ol>

    <h2>Add Subscriber</h2>


    <form role="form" class="form-horizontal form-groups-bordered" action="{{ path('admin_newsletter_subscribe_new') }}" method="post" {{ form_enctype(form) }}>

        <div class="row">
            <div class="col-md-12">

                <div class="panel panel-primary" data-collapsed="0">

                    <div class="panel-heading">
                        <div class="panel-title">
                            Subscriber
                        </div>
                    </div>

                    <div class="panel-body">
                        {{ form_row(form.active) }}
                        {{ form_row(form.name) }}
                        {{ form_row(form.email) }}
                        {{ form_row(form.groups) }}

                        <div class="form-group">
                            <div class="col-sm-offset-3 col-sm-5">
                                <button type="submit" class="btn btn-default">Submit</button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        {{ form_widget(form._token) }}

    </form>

{% endblock %}
